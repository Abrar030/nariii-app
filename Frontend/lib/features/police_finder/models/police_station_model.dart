class PoliceStation {
  final String name;
  final double lat;
  final double lon;
  final double distance; // in meters

  PoliceStation({
    required this.name,
    required this.lat,
    required this.lon,
    required this.distance,
  });
}